/**
 * Application route paths used for navigation throughout the site.
 */
export const SITE_ROUTES = {
  /** Home / landing page route */
  home: '/',
  /** Projects listing page route */
  projects: '/projects',
}
